{
  "$comment": [
    "What was read, and at which revision, for every column of the comparison table in README.md.",
    "",
    "The table is the one thing this project ships that makes claims about other people's software,",
    "and it goes wrong without anything breaking. `npm run smoke` already checks how OLD it is;",
    "this file makes it checkable whether the other side has MOVED since we looked, which is the",
    "question that actually decides whether a cell still holds. `npm run table-sources` compares",
    "each `sha` against that project's current HEAD and names the columns worth re-reading.",
    "",
    "Adopted from csitte.at, whose rule is 'no matrix cell without our own evidence' — they record",
    "the foreign HEAD before mirroring a row. On 2026-08-26 their recorded SHA for taylorwilsdon",
    "and ours matched, which is how a claim about a third party should be confirmable at all.",
    "",
    "The README's `comparison-table-verified` marker carries the OLDEST `verified` date in this",
    "file, not the newest, and a test enforces exactly that. A table is only as current as its",
    "stalest column; tying the marker to the newest check would let one freshly re-read column",
    "stand in front of the others and — since the age budget in `npm run smoke` is measured from",
    "that same marker — reset the budget for all of them. On 2026-09-07 the taylorwilsdon column",
    "alone moved the marker from 09-03 to 09-07, which is what prompted the rule. So: pulling one",
    "column forward no longer re-dates the table, and the columns that hold the marker back are",
    "named in the test failure.",
    "",
    "`shaBasis` is deliberately part of the record: `recorded` means the SHA was read at the time",
    "of checking, `inferred` means it was reconstructed afterwards from the commit history (safe",
    "only when the repository did not move between the check and the reconstruction). Never write",
    "`recorded` for a SHA nobody wrote down — that is the exact habit this file exists to break."
  ],
  "columns": [
    {
      "column": "Google official",
      "kind": "docs",
      "url": "https://developers.google.com/workspace/gmail/api/reference/mcp",
      "verified": "2026-09-03",
      "note": "Full re-read of the MCP tool reference itself on 2026-09-03 (not the Cloud blog), replacing the 2026-08-31 reading of the same page: still Developer Preview, and the same ten tools by name — create_draft, get_message, get_thread, label_message, label_thread, list_drafts, list_labels, search_threads, unlabel_message, unlabel_thread. No send, no filters, no unsubscribe, no snooze, no query-driven bulk. The page documents no return fields at all, which is why the structured-output and sender-authentication cells both read as not offered / not documented rather than as a denial: nothing there mentions SPF, DKIM, DMARC or Authentication-Results. Every cell in this column checked against that page, not carried over."
    },
    {
      "column": "taylorwilsdon",
      "kind": "repo",
      "repo": "taylorwilsdon/google_workspace_mcp",
      "sha": "ed70fb9068231ee13484bc2d53bdde2451e34d31",
      "shaBasis": "recorded",
      "verified": "2026-09-25",
      "note": "Full re-read of all eleven cells on 2026-08-31 at this revision (the 2026-08-29 entry was a delta check; this replaces it). Read gmail/gmail_tools.py end to end: fourteen Gmail tools (this said sixteen until 2026-09-18 \u2014 see the correction at the end of this note), send_gmail_message present, no snooze, no unsubscribe action (List-Unsubscribe only appended to the exported message body), search_gmail_messages returns ids and links without re-checking labels, batch_modify_gmail_message_labels takes message_ids — an explicit list, not a query — and defaults verify=True, which reads the labels back. auth/credential_store.py CHANGED A CELL: LocalDirectoryCredentialStore writes with file mode 0600 and no encryption, GCSCredentialStore delegates to bucket-level CMEK with 'no app-level key'. The cell said a plain yes, which read as app-level token encryption like ours; now it names both mechanisms. No output_schema anywhere in the repository. Scope coupling unchanged since 2026-08-26 (auth/scopes.py, main.py). He now also sells a managed instance, which does not change the 'you operate the server' row: self-hosting remains fully supported. 2026-09-03: brought forward from the 2026-08-31 revision by reading the diff rather than the files again — eleven commits, and none of them touches a cell. They are an OAuth client-secret-file fallback (auth/client_secrets.py, auth/oauth_config.py, core/server.py, main.py) and label colours on the existing manage_gmail_label tool (gmail/gmail_tools.py, gmail/gmail_helpers.py); the tool count, the scope model, the storage mechanisms and the absence of output_schema are all untouched. The sender-authentication cell was re-checked at this HEAD directly: GMAIL_METADATA_HEADERS still ends at List-Id (lines 42-44, which is also the control that proves the search works), and Authentication-Results, dkim, dmarc and spf have no hit in the file. A diff that leaves a cell's code identical is a stronger record than reading the same lines twice — but it is a different check, and this note says which one was run. 2026-09-07: brought forward to 54b1c56 (v1.26.0), 43 commits on, and the round was prompted by a gap on our side rather than a move on his. auth/permissions.py has carried `--permissions <service>:<level>` since 2026-02-25 and was last touched 2026-07-30; the 2026-08-26 reading covered only auth/scopes.py and main.py and so recorded the least-privilege cell as though --read-only were the one switch that narrows scopes. Read at this revision: auth/permissions.py (cumulative levels readonly, organize, drafts, send, full; get_scopes_for_tools returns BASE_SCOPES plus the level's scopes and overrides both the full and the read-only map), core/tool_registry.py (a tool whose _required_google_scopes fall outside the level is disabled, explicitly without scope-hierarchy expansion, so organize excludes gmail.send) and auth/scopes.py. The cell and the paragraph under the table now name that second mechanism; what stays a caveat is the direction — the level is picked per service, not derived from the enabled tools — and the absence of a request-level guard. The other ten cells were brought forward by diff rather than re-read: gmail/gmail_tools.py moved +247/-61 without adding or removing a tool (attachment size handling, body format, the forward and draft paths), output_schema still has no occurrence anywhere in the repository, and nothing in the diff touches snooze, an unsubscribe action, label re-verification or Authentication-Results. One diff hunk is worth naming although it changes no cell: auth/service_decorator.py now substitutes the full Drive scope for drive.file under domain-wide delegation, which widens rather than narrows — a Drive path, and outside the Gmail scope of this table. 2026-09-08: one more file read at the same revision, on a point raised by csitte.at rather than found here, and it holds. In core/tool_registry.py the function is filter_server_tools — its docstring, not its name, says it removes disabled tools from the server after registration, which is presumably where the name in their report came from; there is no remove_disabled_tools in the file. The tier filter is block 1 and runs unconditionally; the read-only filter is block 3 and carries an explicit 'and not permissions_mode'; the permissions filter is block 4. All of them add into one tools_to_remove set, and block 4 skipping names already in it spares work rather than changing the outcome. So at the TOOL gate --tool-tier and --permissions add up, and only --read-only is displaced — the mirror image of the scope side, where the permission level overrides both scope maps. The startup log reports a single mode (Permissions wins over Read-Only) even when the tier filter also removed tools, which is worth knowing before quoting that line as evidence. No cell moves: ours names the two effects separately. `verified` deliberately stays 2026-09-07 — this was one file on one point, not a round over the eleven cells, and a date made younger by a touch this small would claim a check nobody ran. 2026-09-17: brought forward to fb89c818 by reading the diff rather than the files again — 57 commits, and none of them touches a cell. The Gmail half is entirely the SENDING path: gmail/gmail_helpers.py gains html_newlines_to_br (bare newlines in a caller's HTML body become <br>, with raw-text elements and well-formed markup left alone) and _wrap_signature_html, and gmail/gmail_tools.py applies both in send_gmail_message and draft_gmail_message. No tool added or removed, so the count stands \u2014 recorded here as sixteen, which is wrong; it is fourteen, see the correction at the end of this note. The strongest part of this record is what the diff does NOT contain: auth/permissions.py, auth/scopes.py and core/tool_registry.py do not appear in the changed-file list at all, so the least-privilege cell rests on the 2026-09-07 reading with the code provably identical. output_schema still has no occurrence in the repository; nothing touches snooze, an unsubscribe action, label re-verification, token storage or Authentication-Results (gmail/gmail_helpers.py's changed region is the body builder, not GMAIL_METADATA_HEADERS). main.py's +60/-48 is a fatal() helper that routes startup aborts to the logger as well as the startup screen, because the screen is discarded when stderr is not a TTY — an operability change, outside the table. Stars 3112 -> 3179. 2026-09-18: brought forward to e844d805 (v1.27.0) by reading the diff rather than the files again \u2014 16 commits, and none of them touches a cell. The weight of the round is Google Docs and so outside this table: a new plain-text export path (gdocs/docs_plain_text.py, +458), document links (gdocs/docs_links.py) and markdown work, with their tests. The Gmail half of the diff is empty in the strongest sense \u2014 gmail/ does not appear in the changed-file list at all, and neither do auth/permissions.py, auth/scopes.py, core/tool_registry.py or auth/credential_store.py, so the send, sweep, unsubscribe, sender-authentication, least-privilege and token-encryption cells all rest on provably identical code. auth/oauth_config.py is the one auth file that moved (-30): _apply_fastmcp_google_env is gone, which stops mirroring GOOGLE_* into FASTMCP_SERVER_AUTH_GOOGLE_* environment variables. That is provider bootstrap, not a scope map and not a tool gate \u2014 worth naming only because the test that shrank with it is called tests/core/test_http_oauth_scope_config.py and the name reads scope-relevant while the deleted lines are environment cleanup in its setup. Two claims are repository-wide and were therefore checked at the HEAD rather than through the diff: output_schema still has no occurrence anywhere, and neither do snooze, Authentication-Results or dmarc; GMAIL_METADATA_HEADERS still ends at List-Id. CORRECTION, and it is ours, not his: this note has said sixteen Gmail tools since 2026-08-31, including \"the count of sixteen stands\" on 2026-09-17. gmail/gmail_tools.py carries FOURTEEN @server.tool decorators, and it carried fourteen at cce49e11 (2026-08-31), 54b1c56 (2026-09-07) and fb89c818 (2026-09-17) \u2014 counted at each of those revisions, not inferred from this one. No Gmail tool is registered outside that file; the two @server.tool hits in gmail_helpers.py are docstrings explaining what was deliberately kept out of the wrapper. So the miscount sits in the \"end to end\" reading of 2026-08-31, and every diff round since then carried it forward unexamined because a diff can only say that a number did not change. The diff method lost nothing here; the full read did. No cell moves either way \u2014 the table never named a count, which is also why it survived eighteen days and three bring-forward rounds. Stars 3179 -> 3186. 2026-09-20: brought forward to d80ddd92 (v1.27.1) by reading the diff, 11 commits on, and no cell moves - but unlike the last two rounds this diff is not empty where it matters: auth/permissions.py and auth/scopes.py both moved, and those are the two files the least-privilege cell rests on. The change is confined to the chat service. CHAT_MEMBERSHIPS_READONLY_SCOPE is new, and both chat's readonly level in the cumulative table and the chat entry of the read-only scope map now carry it alongside CONTACTS_READONLY_SCOPE; his own comments say the memberships scope names DMs after their members and the People scope resolves sender and member names. The gmail entry in the level table is unchanged (readonly, organize, drafts, send, full), get_scopes_for_tools is untouched, and core/tool_registry.py does not appear in the changed-file list at all, so the cell and the caveat under it stand. One directional point worth recording although it moves no cell: a level named readonly now pulls in a scope of a different API, so the level names the intent of the service rather than the reach of the grant - which is the caveat our own cell already makes, now visible in his code instead of only in ours. The rest of the round is Google Chat (gchat/chat_helpers.py new at +204, gchat/chat_tools.py, three test files) plus a uv.lock refresh. gmail/ does not appear in the diff, so the send, sweep, unsubscribe, sender-authentication and token-encryption cells rest on provably identical code; the repository-wide claims were nevertheless checked at this HEAD in a full checkout rather than through the diff, because a diff cannot carry them: output_schema and outputSchema have no occurrence anywhere, and neither do snooze, Authentication-Results, dmarc or dkim; GMAIL_METADATA_HEADERS still ends at List-Id; gmail/gmail_tools.py still carries fourteen @server.tool decorators, counted again at this revision rather than carried forward from the 2026-09-18 correction; and the only unsubscribe hits are the List-Unsubscribe header and LOW_VALUE_TEXT_FOOTER_MARKERS, a footer heuristic for body extraction, so there is still no unsubscribe action. Stars 3186 -> 3193. 2026-09-22: brought forward to 8475cecf (the v1.28.0 release commit; pyproject.toml says 1.28.0, while the newest GitHub release object at reading time was still v1.27.1) by reading the diff, 33 commits on, and no cell moves. gmail/gmail_tools.py moved +116/-21 without adding or removing a tool. Three changes, all inside existing tools: manage_gmail_label's two visibility parameters become Optional, so an update no longer resets them to the create defaults (a PUT replaces the label outright, and omitted fields are now carried over from the fetched label); the draft path raises UserInputError on a 400/404 for thread_id instead of silently creating an orphan draft that reports success; and a new _render_attached_messages renders message/rfc822 parts (forward-as-attachment, bounces, digests) inside the read tools, capped at five messages and three levels deep, with attachments inside them flagged inAttachedMessage. That renderer labels the wrapped headers 'as claimed by the attachment, unverified' - an honest caveat, recorded because it is the closest the repository comes to the subject of the sender-authentication cell: it names which headers are unverified, and still verifies none. The rest of the round is Sheets named ranges (gsheets/sheets_tools.py +289, a new manage_named_range tool, which is the one added line in core/tool_tiers.yaml) and Office-XML extraction limits in core/utils.py and core/file_limits.py, validated at startup in main.py and fastmcp_server.py. auth/permissions.py, auth/scopes.py, core/tool_registry.py and auth/credential_store.py do not appear in the changed-file list, so the least-privilege and token-encryption cells rest on provably identical code; tests/test_main_permissions_tier.py did grow by 33 lines, and as on 2026-09-18 the name reads scope-relevant while the added tests are file-limit validation at startup. Repository-wide claims checked at this HEAD in a full checkout rather than through the diff: output_schema and outputSchema have no occurrence anywhere, and neither do snooze, Authentication-Results, dmarc or dkim; GMAIL_METADATA_HEADERS still ends at List-Id; gmail/gmail_tools.py still carries fourteen @server.tool decorators, counted again at this revision; the only unsubscribe hits are the List-Unsubscribe header and LOW_VALUE_TEXT_FOOTER_MARKERS, so there is still no unsubscribe action. Stars 3193 -> 3207. 2026-09-25: brought forward to ed70fb90 (v1.29.0) by reading the diff, 84 commits on, and no cell moves - but the two scope files moved again, and this time they WIDEN: auth/permissions.py adds DRIVE_FILE_SCOPE to the sheets full level and replaces the appscript level's DRIVE_FILE_SCOPE with the full DRIVE_SCOPE (his comment: required for manage_script_project delete); auth/scopes.py mirrors both in SHEETS_SCOPES and the script scope list. The gmail entry (readonly, organize, drafts, send, full) is unchanged and core/tool_registry.py is absent from the diff, so the cell stands - and the direction point recorded on 2026-09-20 now has a second instance: a level named for one service reaches into another API, and wider than before. gmail/gmail_tools.py +21/-8 without a tool added or removed: get_gmail_message_content gains format='metadata' (headers only, validated against body_format and full), manage_gmail_label accepts labelShowIfUnread. The new core/portable_schema_middleware.py rewrites INPUT schemas on tools/list (null unions and const dropped for Gemini, issue #1099) - it is not an output schema, and output_schema and outputSchema still have no occurrence at the HEAD. core/tool_tiers.yaml consolidates the Apps Script tools and adds insert_smart_chips - none of it Gmail. Checked in a full checkout: no snooze, Authentication-Results, dmarc or dkim; GMAIL_METADATA_HEADERS still ends at List-Id; fourteen @server.tool decorators counted again at this revision; the unsubscribe hits are unchanged. tests/core/test_http_oauth_scope_config.py grew by seven lines for a refresh-token-expiry setting - once more a scope-sounding name over token-lifetime plumbing. Stars 3207 -> 3224."
    },
    {
      "column": "a-bonus",
      "kind": "repo",
      "repo": "a-bonus/google-docs-mcp",
      "sha": "2e40593d875c1337f754b3ff7235af8d710e4cb3",
      "shaBasis": "recorded",
      "verified": "2026-09-25",
      "note": "Full re-read of all eleven cells on 2026-08-31 at this revision; SHA now recorded rather than inferred. Fourteen files under src/tools/gmail: sendEmail and sendDraft present, no snooze, no unsubscribe, no filter tools. modifyMessageLabels takes ONE messageId — there is no bulk path at all, by ids or by query. listMessages passes q and labelIds straight to messages.list and returns the labels without checking them against the query. triageInbox does carry heuristic per-message signals (newsletter via List-Unsubscribe, meeting keywords, question mark, action requested) plus aggregate stats, so that cell stands. auth.ts has a fixed SCOPES array — no tiers, no derivation. No outputSchema in the repository. No token encryption: firestoreTokenStorage.ts has no cipher; the AES in downloadProxy.ts is for download links, not tokens. 2026-09-03: brought forward from the 2026-08-31 revision by reading the diff — five commits, none of which touches src/tools/gmail or src/auth.ts. They add a batchApplyTextStyle Docs tool and adjust Drive and Sheets listings; the only shared file they touch is src/types.ts, and the 33 added lines there are the Docs batch-style schema. So every Gmail cell, the fixed SCOPES array and the absence of token encryption stand on the 2026-08-31 reading, unchanged in code. The sender-authentication cell holds for the same reason: no Gmail file moved. 2026-09-25: brought forward to 2e40593d by reading the diff - six commits on 2026-09-22 (three pull requests by lemmy-code), none touching src/tools/gmail. src/auth.ts +88/-12 is credential-loading error text (describeMissingCredentials, extractClientSecrets, issue #57); the fixed SCOPES array is untouched, seven scopes with gmail.modify among them. src/index.ts now defers initializeGoogleClient until the stdio transport is up, so the MCP initialize handshake succeeds without credentials; src/tools/drive/savePathGuard.ts restores the downloadFile save-path boundary (#146) - Drive, outside this table. Checked at the HEAD in a full checkout: no outputSchema anywhere; the only cipher is still downloadProxy.ts; snooze has no hit; the unsubscribe hits are the List-Unsubscribe heuristic in triageInbox.ts and its README. No cell moves. Stars 663."
    },
    {
      "column": "aaronsb",
      "kind": "repo",
      "repo": "aaronsb/google-workspace-mcp",
      "sha": "ccdf50784aaf76b3a3337f6e78564d124c6ff6fc",
      "shaBasis": "recorded",
      "verified": "2026-09-08",
      "note": "First reading of this column, added on 2026-09-08 — the date we read, at the revision the repository stood at. It was found by a landscape scan rather than by any cell moving, and the finding is about our own selection: at 176 stars it is an order of magnitude larger than the klodr column we already carried, actively maintained, TypeScript, Apache-2.0. It had been passed over because it is a Workspace suite rather than a Gmail server, which is a reason to describe it carefully, not a reason to omit it. Read at this revision: src/factory/manifest/gmail.yaml end to end (fifteen Gmail operations — search, read, archive, send, reply, replyAll, forward, triage, trash, untrash, getAttachment, viewAttachment, modify, labels, threads, getThread — so send, reply and forward are all present), src/factory/safety.ts, src/accounts/credentials.ts, src/server/batch.ts. The send row is the one that made this column worth adding: safety.ts defines a `draft-only-email` policy blocking send/reply/replyAll/forward, and a `no-delete` policy blocking permanent deletion — the only other server on this table that will refuse to send at all. Both are opt-in through GWS_SAFETY_POLICY, so the default is a sending server and the promise is an operator's environment variable, which is why the cell reads `sends` first. Bulk is by explicit ids, not by query: batch.ts maps `users.messages.batchModify` over `items` carrying a messageId (`ids: items.map(...)`), with no query path anywhere. `triage` is a fixed `messages.list` with `q: 'is:unread in:inbox'` plus a per-message hydration walk — a list, with no bucketing or aggregate stats, hence the qualified cell rather than a tick. `search` is `messages.list` with hydration via `messages.get` at format=metadata; the returned labels are never re-checked against the query, so the re-verification cell is a dash. It goes through messages.list rather than threads.list, which is the endpoint our own measurement found the drift in — worth recording, because it means the cell describes a missing check and not an observed failure. No unsubscribe, no snooze, no filter tool: `snooze`, `unsubscribe`, `Authentication-Results`, `dkim` and `dmarc` have no hit in the repository, and the only occurrence of `filter` in gmail.yaml is a comment — `users.settings.filters.*` appears solely in src/google/descriptor.json, which is the generated catalogue of what Google offers, not of what the server registers. No output_schema or outputSchema anywhere. credentials.ts writes the token as plain JSON at mode 0600 with no cipher, so the encryption cell is a dash like the other community columns. The least-privilege cell names something genuinely different from ours and worth watching: ADR-202 records a per-ACCOUNT read/readwrite level, and safety.ts enforces it at call time against the scopes Google actually granted — read from the credential file, checked against Google's own per-method scope list in the generated descriptor rather than a hand-kept table. Its direction is the same as ours; its position is later, at the call rather than at registration. One limitation the file states about itself, which is why our own guard sits where it does: the policy check runs in the factory handler pipeline, so hand-registered tools get none for free — `manage_scratchpad` had to call it explicitly after shipping without it (the comment names this as how scratchpad went uncovered)."
    },
    {
      "column": "klodr",
      "kind": "repo",
      "repo": "klodr/gmail-mcp",
      "sha": "2fa333bd80c6c44990a59b1db9545732469543b3",
      "shaBasis": "recorded",
      "verified": "2026-09-25",
      "note": "Full re-read of all eleven cells on 2026-08-31 at this revision; SHA now recorded rather than inferred. Thirty-five tool names across src/tools/*: send_email, send_draft, reply_all and forward_email present, no snooze, no unsubscribe. batch_modify_emails takes args.messageIds, so bulk is by explicit id list. get_inbox_with_threads passes q to threads.list and returns summaries — no bucketing, no label re-check. Filter CRUD is complete (create/list/get/delete plus create_filter_from_template). Structured outputs still exactly one: src/tools/output-schemas.ts defines only downloadEmailOutputSchema, and only the download_email registration passes it to defineTool — the file still calls that the 'first wave'. No token encryption (createCipheriv/aes-256 absent repo-wide; oauth-flow.ts chmods to 0o600). scopes.ts hasScope() gates tools by the scopes already granted — the inverse of our direction. 2026-09-03: main is still 59ced418 (2026-07-27); the repository's pushed_at of 2026-09-03 is Dependabot branches, not the default branch, so nothing here moved. The sender-authentication cell was checked at this same revision: no hit for Authentication-Results, dkim, dmarc or spf. 2026-09-25: main moved for the first time since 2026-07-27 - two commits on 2026-09-25, to 2fa333bd (#298 unblocks OSV-Scanner and EditorConfig, #302 adds download_all_attachments). ONE CELL MOVES, structured outputs: src/tools/output-schemas.ts gains downloadAllAttachmentsOutputSchema, and src/tools/downloads.ts passes it into the new defineTool registration exactly as the existing one passes downloadEmailOutputSchema, so the count is two tools now - download_email and download_all_attachments - out of thirty-six (thirty-five before; the new tool sits in src/tools.ts with scopes gmail.readonly|gmail.modify and readOnlyHint, writes only inside the download jail, optionally as one ZIP via fflate). The file's own comment still calls download_email the first wave, so 'more planned' stands. Nothing else moves: batch_modify_emails still takes args.messageIds, hasScope gating is unchanged, no cipher in src/, no snooze. CORRECTION of our own 2026-09-03 sentence above, 'no hit for Authentication-Results, dkim, dmarc or spf': src/prompts.ts line 178 carries 'DMARC/SPF failure hints in from / return-path mismatch' - a phishing-triage prompt that asks the model to guess from a header mismatch, not a read of Authentication-Results. The file is not in this diff, so the line was there on 2026-09-03 and a case-sensitive grep missed it. The cell stays a dash for the reason that matters - no header is read and no value verified - but the note's claim of zero occurrences was wrong, and it is corrected here rather than rewritten. The other unsubscribe hits are prompt text in the same file. Stars 12."
    }
  ]
}
